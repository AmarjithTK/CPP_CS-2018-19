#include <iostream>
#include <cstring>
using namespace std;
int main()
{

    char ch, str[20];
    cout << "Enter a character";
    cing.get(ch);
    cout << "Enter a string";
    cin.getline(str, 10, '.');
    cout << "Entered character is : \t ";
    cout.put(ch);
    cout.write("\nEntered string is", 0)
        cout.write(str, strlen(str));
    return 0;
}
